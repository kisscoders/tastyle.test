import styled from "styled-components";
import { Card } from "react-bootstrap";

export const CardHeader1 = styled(Card.Header)`
  font-weight: 700;
  font-size: 35px;
  background: none;
  border: none;
  text-align: center;
`;
